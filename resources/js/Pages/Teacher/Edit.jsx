import Authenticated from "@/Layouts/AuthenticatedLayout";

export default function Edit({teacher}){

    console.log(teacher);
    
    return(
        <Authenticated>
            Edit
        </Authenticated>
    )
}